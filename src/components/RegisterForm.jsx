import React, { useState } from 'react';const RegisterForm = ({ name, email, password, onChangeName, onChangeEmail, onChangePassword, onRegister, loading, error }) => {const handleRegister = async (event) => {event.preventDefault();onRegister(event);};return (<form onSubmit={handleRegister}><div className="mb-3"><label className="form-label">Name</label><input type="text" className="form-control" value={name} onChange={onChangeName} /></div><div className="mb-3"><label className="form-label">Email</label><input type="email" className="form-control" value={email} onChange={onChangeEmail} /></div><div className="mb-3"><label className="form-label">Password</label><input type="password" className="form-control" value={password} onChange={onChangePassword} /></div><button type="submit" className="btn btn-primary" style={{backgroundColor: '#3498db'}} disabled={loading}>Register</button>{error && <div className="alert alert-danger" role="alert">{error.message}</div>}</form>);};export default RegisterForm;