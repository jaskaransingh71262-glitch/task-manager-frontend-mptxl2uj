import React, { useState } from 'react';const TaskForm = ({ onCreateTask }) => {const [name, setName] = useState('');const [description, setDescription] = useState('');const [loading, setLoading] = useState(false);const [error, setError] = useState(null);const handleCreateTask = async (event) => {event.preventDefault();try {const response = await onCreateTask({ name, description });setName('');setDescription('');} catch (error) {setError(error);setLoading(false);}};return (<form onSubmit={handleCreateTask}><div className="mb-3"><label className="form-label">Name</label><input type="text" className="form-control" value={name} onChange={(event) => setName(event.target.value)} /></div><div className="mb-3"><label className="form-label">Description</label><textarea className="form-control" value={description} onChange={(event) => setDescription(event.target.value)} /></div><button type="submit" className="btn btn-primary" style={{backgroundColor: '#3498db'}} disabled={loading}>Create Task</button></form>);};export default TaskForm;