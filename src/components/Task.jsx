import React from 'react';const Task = ({ tasks, onUpdateTask, onDeleteTask }) => {return tasks.map((task) => (<li key={task.id} className="list-group-item"><div className="d-flex justify-content-between"><span>{task.name}</span><button className="btn btn-sm btn-primary" style={{backgroundColor: '#3498db'}} onClick={() => onUpdateTask(task.id, { completed: !task.completed })}>{task.completed ? 'Mark as incomplete' : 'Mark as completed'}</button><button className="btn btn-sm btn-danger" onClick={() => onDeleteTask(task.id)}>Delete</button></div></li>));};export default Task;